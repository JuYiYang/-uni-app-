export type menuStyleTypeConfig = {
	width: number;
	height: number;
	top: number;
	right: number;
	bottom: number;
	left: number;
};
export type userInfoConfig = {
	id?: string | null,
	email?: string | null,
	header?: string | null,
	nickname?: string | null,
	phone?: string | null,
	phone_model?: string | null,
	token?: string | null,
	username?: string | null,
}
